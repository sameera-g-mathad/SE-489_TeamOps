# on:
#   push:
#     branches:
#       - main
#   # pull_request:
#   #   branches:
#   #     - main

# jobs:
#   test:
#     name: Run Tests
#     runs-on: ${{ matrix.os }}
#     env:
#       AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#       AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#     strategy:
#       matrix:
#         os: ['ubuntu-latest', 'windows-latest', 'macos-latest']

#     steps:
#       - name: 'Checkout'
#         uses: actions/checkout@v4

#       - name: 'Setup python'
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#           cache: 'pip'
#       - name: 'Install Dependencies'
#         run: pip install .

#       - name: 'Pull data from aws s3'
#         run: dvc pull -r data-remote --force

#       - name: 'Pull model from aws s3'
#         run: dvc pull -r model-remote --force

#       - name: 'Execute tests'
#         run: |
#           pytest -v -s tests -W ignore::DeprecationWarning

on:
  push:
    branches:
      - main
  # pull_request:
  #   branches:
  #     - main

jobs:
  test:
    name: Run Tests
    runs-on: ${{ matrix.os }}
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    strategy:
      matrix:
        os: ['ubuntu-latest', 'windows-latest', 'macos-latest']

    steps:
      - name: 'Checkout'
        uses: actions/checkout@v4

      - name: 'Setup Python'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 'Install dependencies'
        run: pip install .

      - name: 'Pull data from DVC'
        run: dvc pull data/ --force

      - name: 'Pull models from DVC'
        run: dvc pull models/ --force

      - name: 'Run tests with pytest'
        run: |
          pytest -v -s tests -W ignore::DeprecationWarning
